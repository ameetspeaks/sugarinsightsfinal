enum TimeFilter {
  today,
  weekly,
  monthly,
}

enum GlucoseType {
  fasting,
  postMeal,
}

enum NavItem {
  home,
  medicine,
  diet,
  education,
  profile,
}

enum HealthStatus {
  normal,
  high,
  low,
  noData,
} 